@model IEnumerable<WebBanHangOnline.Models.EF.Posts>
@{
    ViewBag.Title = "Bài viết";
}
<link rel="stylesheet" type="text/css" href="~/Content/assets/styles/main_styles.css">

@if (Model != null && Model.Any())
{

    <div class="container" style="margin-top: 200px; margin-left: 300px;">
        @foreach (var item in Model)
        {
            <div style="margin-top: 200px" ;>
                @*<div class="col-lg-12 post">
            <div class="blog_item">
                <div><img src="@item.Image" /></div>
                <div class="blog_content d-flex flex-column align-items-center justify-content-center text-center">
                    <h4 class="blog_title">@item.Title</h4>
                    <span class="blog_meta">by @item.CreatedBy | @item.CreatedDate.ToString("MMM dd,yyyy")</span>
                    <a class="blog_more" href="/chi-tiet-bai-viet-@item.Id">Read more</a>
                </div>
            </div>
        </div>*@
                <div class="col-lg-10 col-md-10 col-sm-12 col-xs-12" style="margin-left:250px"> 
                    <div class="single-post">
                        <div class="post-header mb-5 text-center">
                            <h2 class="post-title mt-2">
                                @item.Title
                            </h2>

                            <div class="post-meta">
                                <span class="text-uppercase font-sm letter-spacing-1 mr-3">by @item.CreatedBy</span>
                                <span class="text-uppercase font-sm letter-spacing-1">@item.CreatedDate.ToString("MMM dd,yyyy")</span>
                            </div>   <a class="blog_more" href="/chi-tiet-bai-viet-@item.Id">chi tiết</a>
                            <div class="post-featured-image mt-5">
                                <img src="@item.Image" class="img-fluid w-80" alt="featured-image">

                            </div>

                        </div>
                    </div>
                </div>
            </div>
            <hr  class="align-content-center"/>
        }


    </div>

}